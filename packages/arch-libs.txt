# Bibliotecas e utilitários que praticamente todo sistema precisa
# Todos verificados nos repositórios oficiais do CachyOS.
# Linhas em branco e comentários (#) são ignorados pelo instalador.

# === Documentação / conveniência do shell ===
man-db
man-pages
bash-completion

# === Arquivos compactados (zip, rar, 7z, etc.) ===
7zip
unzip
zip
unrar
unarchiver

# === Montagem de dispositivos e sistemas de arquivos ===
gvfs
gvfs-mtp
gvfs-smb
gvfs-nfs
gvfs-afc
ntfs-3g
exfatprogs
dosfstools

# === Diagnóstico de hardware / sistema ===
usbutils
pciutils
lsof

# Controle de brilho via DDC/CI (monitores externos) — usado pelo Noctalia
# Shell para o slider/OSD de brilho funcionar em monitores que não expõem
# backlight por ACPI (a maioria dos externos via HDMI/DisplayPort).
ddcutil

# === Rede / mirrors / gráficos ===
reflector
vulkan-icd-loader
