# Pacotes base COMUNS do ambiente (Arch Linux) — independentes do compositor
# Shell + Terminal + Apps essenciais compartilhados por Niri e Hyprland.
# O compositor em si (niri / hyprland) é instalado à parte, conforme a escolha
# do usuário — veja packages/arch-niri.txt e packages/arch-hyprland.txt.
# Linhas em branco e comentários (#) são ignorados pelo instalador

# === Base do XDG Desktop Portal (comum aos dois compositores) ===
# O portal específico do compositor (gnome p/ Niri, hyprland p/ Hyprland)
# é instalado junto do compositor escolhido.
xdg-desktop-portal
xdg-desktop-portal-gtk

# === Shell de Desktop ===
# O shell (DMS ou Noctalia) é instalado condicionalmente pelo instalador,
# conforme a escolha do usuário — veja install_shell_packages() em lib/packages.sh.
matugen

# === Terminais ===
alacritty
ghostty

# === Launcher e Utilitários Wayland ===
fuzzel
wl-clipboard
grim
slurp

# === Shell (linha de comando) ===
fish

# === Editores ===
micro

# === Monitor / Sistema ===
btop
fastfetch
cava

# === Ferramentas de busca / sistema ===
ripgrep
rsync
playerctl
git

# === Bluetooth ===
bluez
bluez-utils

# === Rede ===
networkmanager

# === Aplicativos GUI ===
gnome-disk-utility
mpv
keepassxc
flatpak
gnome-text-editor
nautilus
# firefox foi movido para o menu de navegadores (packages/arch-browsers.txt)

# === Audio (PipeWire) ===
pipewire
pipewire-pulse
pipewire-alsa
wireplumber
pavucontrol

# === Integração de sistema ===
xdg-utils
libsecret
polkit-gnome
gnome-keyring
seahorse
power-profiles-daemon
ufw

# === Multimídia / Thumbnails ===
gst-libav
gst-plugins-good
gst-plugins-bad
gst-plugins-ugly
ffmpeg
ffmpegthumbnailer
tumbler
poppler-glib

# === Compartilhamento / MIME ===
shared-mime-info
libappindicator-gtk3
